import { Title } from '../components/Title/Title';

export function Dashboard() {
    return (
        <div>
            <Title>Title</Title>
            // This is the Dashboard component y al añadir children al componente, tenemos que completarlo con la etiqueta de cierre
        </div>
    );
}